@model IEnumerable<ProjetGestioStock.Models.TblSortie>

@{
    ViewData["Title"] = "Month";
}

<header class="mb-3">
    <a href="#" class="burger-btn d-block d-xl-none">
        <i class="bi bi-justify fs-3"></i>
    </a>
</header>

<div class="page-heading">

    <!-- Basic Tables start -->
    <section class="section">
        <div class="card">
            <div class="card-header">
                Jquery Datatable
            </div>
            <div class="card-body">
                <table class="table" id="table1">
                    <thead>
                        <tr>
                            <th>Produit</th>
                            <th>Quantite</th>
                            <th>Montant</th>
                            <th>Date sortie</th>
                            <th>Client</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Designation)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Qte)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Montant)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Dateday)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.NomClient)
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </section>
    <!-- Basic Tables end -->
</div>
